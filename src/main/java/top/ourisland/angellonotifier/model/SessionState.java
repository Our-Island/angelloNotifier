package top.ourisland.angellonotifier.model;

public class SessionState {

    boolean joinProcessedOnEligibleServer;

    public boolean joinProcessedOnEligibleServer() {
        return joinProcessedOnEligibleServer;
    }

    public void joinProcessedOnEligibleServer(boolean joinProcessedOnEligibleServer) {
        this.joinProcessedOnEligibleServer = joinProcessedOnEligibleServer;
    }

}
